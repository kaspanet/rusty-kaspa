use hashes::Hash;

pub struct ParentsBuilder {}

impl ParentsBuilder {
    pub fn calc_block_parents(pruning_point: Hash, direct_parents: Vec<Hash>) -> Vec<Vec<Hash>> {
        Default::default()
    }
}

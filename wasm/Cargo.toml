[package]
name = "keryx-wasm"
description = "KASPA WASM bindings"
rust-version.workspace = true
version.workspace = true
edition.workspace = true
authors.workspace = true
include.workspace = true
repository.workspace = true
# wasm-pack requires this value to be a string
# (it can not be derived from the workspace)
license = "ISC"

[lib]
crate-type = ["cdylib"]

[dependencies]
cfg-if.workspace = true
js-sys.workspace = true
keryx-addresses.workspace = true
keryx-bip32.workspace = true
keryx-consensus-core.workspace = true
keryx-consensus-wasm.workspace = true
keryx-core.workspace = true
keryx-math.workspace = true
keryx-pow.workspace = true
keryx-txscript.workspace = true
keryx-rpc-core.workspace = true
keryx-utils.workspace = true
keryx-wasm-core.workspace = true
keryx-wallet-core.workspace = true
keryx-wallet-keys.workspace = true
keryx-wrpc-client.workspace = true
keryx-wrpc-wasm.workspace = true
num.workspace = true
wasm-bindgen.workspace = true
workflow-log.workspace = true
workflow-core.workspace = true
workflow-wasm.workspace = true

[features]
wasm32-sdk = [
    "keryx-wallet-core/wasm32-sdk",
    "keryx-pow/wasm32-sdk",
    "keryx-txscript/wasm32-sdk",
]
wasm32-core = [
    "keryx-wallet-core/wasm32-core",
    "keryx-pow/wasm32-sdk",
    "keryx-txscript/wasm32-sdk",
]
wasm32-rpc = [
    "keryx-consensus-core/wasm32-sdk",
    "keryx-consensus-wasm/wasm32-sdk",
    "keryx-wrpc-client/wasm32-sdk",
    "keryx-wrpc-wasm/wasm32-sdk",
]
wasm32-keygen = []
default = []

[package.metadata.docs.rs]
targets = ["wasm32-unknown-unknown"]

[package.metadata.emanate.wasm]
targets = [
    { target = "nodejs", out-dir = "nodejs/kaspa" },
    { target = "web", out-dir = "web/kaspa" },
]
folder = "setup"

# Workaround for Rust 1.87.0
# https://github.com/rust-lang/rust/issues/141048
[package.metadata.wasm-pack.profile.release]
# This optimization configuration requires "-Ctarget-cpu=mvp" Rustflag (only enable W3C MVP feature set)
# This flag is automatically used either via (release optimized) build script at root
#   or via .cargo/config.toml when executing the build from /wasm
wasm-opt = ["-O","--enable-bulk-memory"]

[lints]
workspace = true

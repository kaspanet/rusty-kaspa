pub enum Creator {}
pub enum Constructor {}

pub enum Updater {}

pub enum Signer {}

pub enum Combiner {}

pub enum Finalizer {}

pub enum Extractor {} // todo use script engine to validate tx, determine lock time